services:
  kws2100-database:
    container_name: kws2100
    image: postgis/postgis
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
