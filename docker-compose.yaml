services:
  database:
    image: postgis/postgis
    container_name: lecture10
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_HOST_AUTH_METHOD: trust
