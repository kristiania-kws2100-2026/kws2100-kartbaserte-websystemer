services:
  kws2100_db:
    container_name: kws2100_database
    image: postgis/postgis
    environment:
      POSTGRES_PASSWORD: "passord"
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
